{
  "name": "geupsik",
  "version": "1.0.0",
  "main": "app.js",
  "repository": "https://github.com/sungho0205/geupsik",
  "author": "sungho0205 <moon@7959.ml>",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html",
    "build": "yarn clean && parcel build index.html robots.txt --no-minify && sed -i '' 's/parcelRequire = (/var parcelRequire = (/g' dist/app.*.js && npx netlify deploy --prod --dir dist",
    "clean": "rm -rf dist",
    "git": "git add . && git commit -m 'd' && git push origin master"
  },
  "dependencies": {
    "date-fns": "^2.24.0",
    "sweetalert": "^2.1.2",
    "sweetalert2": "^11.1.6"
  },
  "devDependencies": {
    "cssnano": "^5.0.8",
    "parcel-bundler": "^1.12.5",
    "postcss": "^8.2.15"
  }
}
